# nginx.conf

A+ 을 받기 위해!

```bash
# pbzweihander.me
sudo ln -sf "$PWD/trude.conf" /etc/nginx/nginx.conf
sudo ln -sf "$PWD/options-ssl-nginx.conf" /etc/letsencrypt/

# helma.pbzweihander.me
sudo ln -sf "$PWD/helma.conf" /etc/nginx/nginx.conf
sudo ln -sf "$PWD/options-ssl-nginx.conf" /etc/letsencrypt/
```

